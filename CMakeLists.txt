PROJECT(bingo)

SET(PROJECT_SRCS
    bingo.cpp
    persistence.cpp
)

SET(PROJECT_HDRS
    bingo.h
)

SET_SOURCE_FILES_PROPERTIES(${PROJECT_HDRS} PROPERTIES HEADER_FILE_ONLY TRUE)

LIST(APPEND PROJECT_SRCS ${PROJECT_HDRS})

DFHACK_PLUGIN(bingo ${PROJECT_SRCS} COMPILE_FLAGS_GCC "-Wall -Wextra -Werror" COMPILE_FLAGS_MSVC "/W3")
